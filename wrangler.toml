name = "deutsch-mit-ralph"
compatibility_date = "2025-01-01"

# Publica o build do Vite (pasta dist) como site estático (SPA).
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
